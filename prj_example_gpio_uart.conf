# 可选：与默认 prj.conf 合并，启用 GPIO / UART 示例模块
# west build -b <board> -DCONF_FILE="prj.conf;prj_example_gpio_uart.conf" .
#
# 需要板级 devicetree 提供 led0；按键可选 sw0（无 sw0 时示例会关闭按键功能）。

CONFIG_EXAMPLE_MODULE_GPIO=y
CONFIG_EXAMPLE_MODULE_UART=y
